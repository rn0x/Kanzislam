extends ../layout.pug

block stylesheet
    link(rel="stylesheet" href="/css/forum.css")

block content
    #forumPage
        ul.breadcrumb
            li 
                a(href="/" title="الرئيسية")
                    img(src="/icon/home.svg" alt="الرئيسية" class="iconFilter") 
            li 
                small /
                a(href="/forum" title=`مجتمع ${options.website_name}`) مجتمع #{options.website_name}
            li 
                small /
                a(href=`/forum/${options.getCategorie.category_id}` title=options.getCategorie.title) #{options.getCategorie.title}
        h2 #{options.getCategorie.title}
        .categorieTopics
            button(title="إنشاء موضوع جديد")#buttonAddTopics موضوع جديد
            table.topicsTable 
                thead
                    tr 
                        th.topics الموضوع
                        th.topicsCount.likes الإعجابات
                        th.topicsCount.views المشاهدات
                        th.topicsCount.comments التعليقات
                tbody#topiscTbody
                    //- tr 
                    //-     td.topicsMain
                    //-         div 
                    //-             a(href="" title="title topic") عنوان الموضوع
                    //-             .topicUsername 
                    //-                img(src="/icon/user.svg", alt="username")
                    //-                a(href="") username
                    //-             small.topicsTime منذ 5 دقائق

                    //-     td.likes 5
                    //-     td.views 466
                    //-     td.comments 12
            button(title="المزيد من المواضيع")#ButtonMore المزيد


block script
    script(type="module")
        include ../../public/js/forum/getCategoryTopics.js
    script.
        window.options = !{jsStringify(options)}; 